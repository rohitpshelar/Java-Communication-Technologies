rootProject.name = 'ct'

//include 'kafka:producer'
['producer','consumer','common'].forEach { proj ->
    {
        include(":kafka:${proj}")
        project(":kafka:${proj}").projectDir = file("kafka/${proj}")
    }
}